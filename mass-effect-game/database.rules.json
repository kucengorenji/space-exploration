{
  "rules": {
    "rooms": {
      "$roomId": {
        "positions": {
          ".read": "auth != null",
          "$uid": {
            ".write": "auth != null && auth.uid === $uid"
          }
        }
      }
    }
  }
}
